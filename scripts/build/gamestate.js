"use strict";
var gameState = /** @class */ (function () {
    function gameState(score, mode, activePowerup, level) {
        this.score = score;
        this.mode = mode;
        this.activePowerup = activePowerup;
        this.level = level;
    }
    return gameState;
}());
